William, perfect — your agent’s correction is right, and the renumbering is the right call.  
So we lock in the sequence:

### **Plan 004 — Round‑Trip Regeneration (complete)**  
### **Plan 005 — v1.1 Manufacturing Semantic Expansion (in progress)**  
### **Plan 006 — Context Graph Integration (next)**

Your agent’s clarifications are also spot‑on:

- Regeneration runs **upsert** artifacts and add new `USED_ARTIFACT` links — no duplicates.  
- Promotion is a **two‑rename swap**, not a single atomic FS op.  
- Ontop validates **synthetic H2 mappings**, not Neo4j provenance.  
- Neo4j provenance is validated via **Cypher**, not Ontop.

All correct.

Now that Plan 005 is underway, let me give you a **clean, agent‑ready Plan 006** — the one your agent will need immediately after finishing semantic expansion.

This is the plan that bridges your SALT manufacturing ontology into **graph‑based agent reasoning**, **contextual memory**, and **domain‑aware tool generation**.

---

# **📘 Plan 006 — Context Graph Integration (Manufacturing Domain)**  
**Purpose:**  
Integrate the SALT manufacturing ontology (v1.1) into a full **context graph domain**, enabling:

- graph‑based reasoning  
- agent memory  
- structured retrieval  
- GDS projections  
- domain‑aware tools  
- document ingestion rules  
- multi‑agent workflows  

This is the step that turns your ontology into a **usable agent substrate**.

---

## **1. Inputs**
- v1.1 ontology snapshot  
- v1.1 regenerated artifacts  
- Neo4j metadata (classes, properties, domain/range)  
- SALT manufacturing semantics (expanded)  
- Your existing ingestion + extraction + regeneration pipeline  
- Create Context Graph scaffolding (from Neo4j Labs)

---

## **2. Outputs**
A complete manufacturing **context graph domain**, including:

### **2.1 Domain YAML**
Defines:

- node types  
- relationship types  
- properties  
- embeddings  
- retrieval rules  
- ingestion rules  
- agent tools  

### **2.2 Schema Cypher**
Creates:

- node labels  
- relationship types  
- constraints  
- indexes  
- projections  

### **2.3 GDS Projections**
For:

- similarity search  
- neighborhood reasoning  
- routing  
- BOM traversal  
- MRP lineage  

### **2.4 Agent Tools**
Auto‑generated tools for:

- retrieving WorkOrders  
- retrieving BOM components  
- retrieving Machines / WorkCenters  
- retrieving ProductionEvents  
- retrieving MaterialRequirements  
- retrieving Suppliers / Parts  

### **2.5 Document Ingestion Rules**
For:

- manufacturing PDFs  
- work instructions  
- routing sheets  
- BOM documents  
- supplier specs  

### **2.6 Context Graph Package**
A folder containing:

```
domain.yaml
schema.cypher
gds.cypher
tools/
ingestion/
validation/
```

This becomes the agent’s “reasoning memory.”

---

## **3. Tasks**

### **Task 6.1 — Generate Domain YAML**
From v1.1 snapshot:

- map classes → node types  
- map properties → relationship types  
- map datatype properties → node attributes  
- define embeddings for text fields  
- define retrieval rules  
- define ingestion rules  
- define agent tools  

### **Task 6.2 — Generate Schema Cypher**
From domain YAML:

- create constraints  
- create indexes  
- create node labels  
- create relationship types  

### **Task 6.3 — Generate GDS Projections**
For:

- BOM graph  
- MRP lineage graph  
- WorkOrder → RoutingStep → Machine graph  
- Supplier → Part graph  

### **Task 6.4 — Generate Agent Tools**
Auto‑generate:

- `get_work_order`  
- `get_bom_components`  
- `get_machine_status`  
- `get_supplier_parts`  
- `get_material_requirements`  
- `get_production_events`  

### **Task 6.5 — Generate Document Ingestion Rules**
Define:

- text extraction  
- chunking  
- embedding  
- linking to graph nodes  
- provenance rules  

### **Task 6.6 — Validate Context Graph**
Run:

- schema.cypher  
- gds.cypher  
- tool tests  
- ingestion tests  
- provenance tests  

### **Task 6.7 — Package Context Graph**
Produce:

```
context-graph/
    domain.yaml
    schema.cypher
    gds.cypher
    tools/
    ingestion/
    validation/
```

---

## **4. Success Criteria**
Plan 006 is complete when:

- domain.yaml is generated from v1.1 snapshot  
- schema.cypher builds a valid graph  
- GDS projections run  
- agent tools work  
- ingestion rules work  
- provenance is correct  
- no SAP data is touched  
- no CDS content is touched  
- no transactional tables are touched  

At that point, your SALT manufacturing ontology becomes a **full agent reasoning substrate**.

---

## **5. Ready for your agent**
This plan is written in the same style your agent already consumes:

- scoped  
- incremental  
- artifact‑driven  
- provenance‑aware  
- reversible  
- safe  

You can hand this directly to your agent as **Plan 006**.

---

If you want, I can also generate:

- the initial `domain.yaml` skeleton  
- the manufacturing node/relationship definitions  
- the GDS projection templates  
- the agent tool definitions  
- the ingestion rule templates  

Just tell me how deep you want the agent’s starting point to be.