

### Recommended next steps

1. **Wire Plan 007 into your agent runtime**
   - Load `domain.yaml`, `tools/manufacturing-tools.yaml`, and `reasoning/strategies.yaml` at startup.
   - Register the six manufacturing tools with your chosen agent framework.
   - Add the strategy dispatcher enforcing:
     - named strategies only  
     - max 4 hops  
     - `$ontology_id = 'salt.manufacturing.v1.1'` on every query.

2. **Implement the reasoning prompt contract**
   - Separate: **observed evidence**, **missing evidence**, **assumptions**.
   - Require clarification when no explicit edge exists.
   - Surface node IDs, relationship types, and document chunk IDs as citations.

3. **Add fixture‑based tests for each strategy**
   - BOM requirements  
   - schedule contention  
   - production lineage  
   - supplier coverage  
   - Negative cases: prove no result is inferred from matching labels/timestamps alone.

4. **Keep GDS artifacts out of the runtime**
   - Treat `reasoning/strategies.yaml` as the canonical reasoning surface.
   - Leave any GDS projections as non‑executing, future‑compatible docs only (if you keep them at all).

