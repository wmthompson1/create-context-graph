PREFIX saltmfg: <https://example.org/ontology/salt/manufacturing/v1.0#>

SELECT ?workOrder ?materialRequirement
WHERE {
  ?workOrder a saltmfg:WorkOrder ;
    saltmfg:requiresMaterial ?materialRequirement .
}
ORDER BY ?workOrder ?materialRequirement